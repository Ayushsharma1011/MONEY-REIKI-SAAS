/** Notification defaults for shared notification infrastructure. */
export const notificationConfig = {
  defaultType: "system",
  digestHour: 9,
  maxTitleLength: 160,
  maxMessageLength: 1000
} as const;
